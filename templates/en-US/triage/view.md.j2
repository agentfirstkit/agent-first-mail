---
kind: triage_view
message_id: {{ message_id }}
message_ids:
  - {{ message_id }}
generated_rfc3339: "{{ generated_rfc3339 }}"
message_count: 1
attachment_count: {{ attachment_count }}
{% if suggested_case_uids | length > 0 %}suggested_case_uids:
{% for case_uid in suggested_case_uids %}  - {{ case_uid }}
{% endfor %}{% if suggested_reason %}suggested_reason: "{{ suggested_reason_yaml }}"
{% endif %}{% endif %}---

# {% if title %}{{ title }}{% else %}Incoming mail{% endif %}
{% if suggested_case_uids | length > 0 %}

Suggested case UIDs: {% for case_uid in suggested_case_uids %}{% if not loop.first %}, {% endif %}`{{ case_uid }}`{% endfor %}
{% endif %}{% if related_messages | length > 0 %}

## Related messages

These messages are linked by RFC headers (`In-Reply-To` / `References`). Add them explicitly to one case before disposition.

| Message | Direction | From | Subject | Time | Workspace status |
| --- | --- | --- | --- | --- | --- |
{% for item in related_messages %}| `{{ item.message_id }}` | {{ item.direction }} | {{ item.from }} | {{ item.subject }} | {{ item.time }} | {{ item.status }} |
{% endfor %}{% endif %}

<!-- afmail:conversation:start -->

{{ conversation }}

<!-- afmail:conversation:end -->
