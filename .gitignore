/target

# macOS metadata files
.DS_Store

# hypha spawn working directory
.cmn

# Local test secrets and toggles
tests/.env.local

# afmail workspace state (never tracked in the source repo)
/.afmail/
