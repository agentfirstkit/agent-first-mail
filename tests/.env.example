# Copy to tests/.env.local for local integration tests.

# Enable the ignored Docker/GreenMail E2E locally:
# cargo test --test container_e2e -- --ignored --nocapture
AFMAIL_E2E=1
AFMAIL_E2E_GREENMAIL_IMAGE=greenmail/standalone:2.1.8
